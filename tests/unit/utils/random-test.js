import random from 'digital-currency/utils/random';
import { module, test } from 'qunit';

module('Unit | Utility | random');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = random();
  assert.ok(result);
});
